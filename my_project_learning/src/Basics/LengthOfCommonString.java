/*Write a program that reads two strings from the user and then finds 
 * the length of the longest common substring between the two strings. 
 * The program should print the length of the longest common substring.
*/

package Basics;

import java.util.Scanner;

public class LengthOfCommonString {

	public static void main(String[] args) {
		Scanner sc=new Scanner(System.in);
		System.out.println("the input string");
		String str="";
		String str1="";
		if(sc.hasNext()) {
			str=sc.nextLine();
			str1=sc.nextLine();		
		}

sc.close();
	}

}


		
		
